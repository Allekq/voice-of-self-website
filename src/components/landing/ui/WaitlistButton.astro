---
import { siteConfig } from "../../../config/site";
import ActionButton from "./ActionButton.astro";

interface Props {
  variant?: "primary" | "secondary";
  className?: string;
  label?: string;
}

const { variant = "primary", className, label } = Astro.props as Props;
---

<ActionButton
  className={className}
  data-waitlist-cta="auto"
  data-waitlist-mobile-href={siteConfig.waitlistMailtoHref}
  data-waitlist-desktop-href={siteConfig.waitlistGmailHref}
  href={siteConfig.waitlistMailtoHref}
  variant={variant}
>
  <slot>{label ?? siteConfig.ctas.primary}</slot>
</ActionButton>
